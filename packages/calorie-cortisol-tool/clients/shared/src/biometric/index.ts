/**
 * Biometric access gate (Task 14.19).
 *
 * Public surface: the gate types/constants/ports and the pure gate logic
 * (`BiometricAccessGate` plus the reducer/helper functions).
 *
 * Requirements: 18.1, 18.3, 18.4, 18.5, 18.6
 */
export * from './types';
export * from './biometric-gate';

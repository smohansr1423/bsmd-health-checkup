import XCTest
@testable import CalorieCortisol

// Scaffolding smoke test verifying the XCTest toolchain is wired.
final class PlaceholderTests: XCTestCase {
    func testPackageName() {
        XCTAssertEqual(CalorieCortisol.packageName, "calorie-cortisol-ios")
    }
}
